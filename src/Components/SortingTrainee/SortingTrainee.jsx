import { useState, useEffect } from 'react';
import { clientAPI } from 'service/axios.config';
import moment from 'moment/moment';
import SortingFirstRender from './SortingFirstRender/SortingFirstRender';
import SortingSecondRender from './SortingSecondRender/SortingSecondRender';
import {
  WrapperBTN,
  WrapperInfoTime,
  // NextBTNPosition,
  // NextBTNWrapper,
  NextDaysBTN
} from './SortingTrainee.styled';

const SortingTrainee = () => {
  const [dataAllClients, setDataAllClients] = useState('');
  const [clientsToday0800, setClientsToday0800] = useState('');
  const [clientsToday0900, setClientsToday0900] = useState('');
  const [clientsToday1000, setClientsToday1000] = useState('');
  const [clientsToday1100, setClientsToday1100] = useState('');
  const [clientsToday1200, setClientsToday1200] = useState('');
  const [clientsToday1300, setClientsToday1300] = useState('');
  const [clientsToday1400, setClientsToday1400] = useState('');
  const [clientsToday1500, setClientsToday1500] = useState('');
  const [clientsToday1600, setClientsToday1600] = useState('');
  const [clientsToday1700, setClientsToday1700] = useState('');
  const [clientsToday1800, setClientsToday1800] = useState('');
  const [clientsToday1900, setClientsToday1900] = useState('');
  const [clientsToday2000, setClientsToday2000] = useState('');
  const [clientsNextDay0800, setClientsNextDay0800] = useState('');
  const [clientsNextDay0900, setClientsNextDay0900] = useState('');
  const [clientsNextDay1000, setClientsNextDay1000] = useState('');
  const [clientsNextDay1100, setClientsNextDay1100] = useState('');
  const [clientsNextDay1200, setClientsNextDay1200] = useState('');
  const [clientsNextDay1300, setClientsNextDay1300] = useState('');
  const [clientsNextDay1400, setClientsNextDay1400] = useState('');
  const [clientsNextDay1500, setClientsNextDay1500] = useState('');
  const [clientsNextDay1600, setClientsNextDay1600] = useState('');
  const [clientsNextDay1700, setClientsNextDay1700] = useState('');
  const [clientsNextDay1800, setClientsNextDay1800] = useState('');
  const [clientsNextDay1900, setClientsNextDay1900] = useState('');
  const [clientsNextDay2000, setClientsNextDay2000] = useState('');
  const [clientsNextDayTwo0800, setClientsNextDayTwo0800] = useState('');
  const [clientsNextDayTwo0900, setClientsNextDayTwo0900] = useState('');
  const [clientsNextDayTwo1000, setClientsNextDayTwo1000] = useState('');
  const [clientsNextDayTwo1100, setClientsNextDayTwo1100] = useState('');
  const [clientsNextDayTwo1200, setClientsNextDayTwo1200] = useState('');
  const [clientsNextDayTwo1300, setClientsNextDayTwo1300] = useState('');
  const [clientsNextDayTwo1400, setClientsNextDayTwo1400] = useState('');
  const [clientsNextDayTwo1500, setClientsNextDayTwo1500] = useState('');
  const [clientsNextDayTwo1600, setClientsNextDayTwo1600] = useState('');
  const [clientsNextDayTwo1700, setClientsNextDayTwo1700] = useState('');
  const [clientsNextDayTwo1800, setClientsNextDayTwo1800] = useState('');
  const [clientsNextDayTwo1900, setClientsNextDayTwo1900] = useState('');
  const [clientsNextDayTwo2000, setClientsNextDayTwo2000] = useState('');
  const [clientsNextDayThree0800, setClientsNextDayThree0800] = useState('');
  const [clientsNextDayThree0900, setClientsNextDayThree0900] = useState('');
  const [clientsNextDayThree1000, setClientsNextDayThree1000] = useState('');
  const [clientsNextDayThree1100, setClientsNextDayThree1100] = useState('');
  const [clientsNextDayThree1200, setClientsNextDayThree1200] = useState('');
  const [clientsNextDayThree1300, setClientsNextDayThree1300] = useState('');
  const [clientsNextDayThree1400, setClientsNextDayThree1400] = useState('');
  const [clientsNextDayThree1500, setClientsNextDayThree1500] = useState('');
  const [clientsNextDayThree1600, setClientsNextDayThree1600] = useState('');
  const [clientsNextDayThree1700, setClientsNextDayThree1700] = useState('');
  const [clientsNextDayThree1800, setClientsNextDayThree1800] = useState('');
  const [clientsNextDayThree1900, setClientsNextDayThree1900] = useState('');
  const [clientsNextDayThree2000, setClientsNextDayThree2000] = useState('');
  const [clientsNextDayFour0800, setClientsNextDayFour0800] = useState('');
  const [clientsNextDayFour0900, setClientsNextDayFour0900] = useState('');
  const [clientsNextDayFour1000, setClientsNextDayFour1000] = useState('');
  const [clientsNextDayFour1100, setClientsNextDayFour1100] = useState('');
  const [clientsNextDayFour1200, setClientsNextDayFour1200] = useState('');
  const [clientsNextDayFour1300, setClientsNextDayFour1300] = useState('');
  const [clientsNextDayFour1400, setClientsNextDayFour1400] = useState('');
  const [clientsNextDayFour1500, setClientsNextDayFour1500] = useState('');
  const [clientsNextDayFour1600, setClientsNextDayFour1600] = useState('');
  const [clientsNextDayFour1700, setClientsNextDayFour1700] = useState('');
  const [clientsNextDayFour1800, setClientsNextDayFour1800] = useState('');
  const [clientsNextDayFour1900, setClientsNextDayFour1900] = useState('');
  const [clientsNextDayFour2000, setClientsNextDayFour2000] = useState('');
  const [clientsNextDayFive0800, setClientsNextDayFive0800] = useState('');
  const [clientsNextDayFive0900, setClientsNextDayFive0900] = useState('');
  const [clientsNextDayFive1000, setClientsNextDayFive1000] = useState('');
  const [clientsNextDayFive1100, setClientsNextDayFive1100] = useState('');
  const [clientsNextDayFive1200, setClientsNextDayFive1200] = useState('');
  const [clientsNextDayFive1300, setClientsNextDayFive1300] = useState('');
  const [clientsNextDayFive1400, setClientsNextDayFive1400] = useState('');
  const [clientsNextDayFive1500, setClientsNextDayFive1500] = useState('');
  const [clientsNextDayFive1600, setClientsNextDayFive1600] = useState('');
  const [clientsNextDayFive1700, setClientsNextDayFive1700] = useState('');
  const [clientsNextDayFive1800, setClientsNextDayFive1800] = useState('');
  const [clientsNextDayFive1900, setClientsNextDayFive1900] = useState('');
  const [clientsNextDayFive2000, setClientsNextDayFive2000] = useState('');
  const [clientsNextDaySix0800, setClientsNextDaySix0800] = useState('');
  const [clientsNextDaySix0900, setClientsNextDaySix0900] = useState('');
  const [clientsNextDaySix1000, setClientsNextDaySix1000] = useState('');
  const [clientsNextDaySix1100, setClientsNextDaySix1100] = useState('');
  const [clientsNextDaySix1200, setClientsNextDaySix1200] = useState('');
  const [clientsNextDaySix1300, setClientsNextDaySix1300] = useState('');
  const [clientsNextDaySix1400, setClientsNextDaySix1400] = useState('');
  const [clientsNextDaySix1500, setClientsNextDaySix1500] = useState('');
  const [clientsNextDaySix1600, setClientsNextDaySix1600] = useState('');
  const [clientsNextDaySix1700, setClientsNextDaySix1700] = useState('');
  const [clientsNextDaySix1800, setClientsNextDaySix1800] = useState('');
  const [clientsNextDaySix1900, setClientsNextDaySix1900] = useState('');
  const [clientsNextDaySix2000, setClientsNextDaySix2000] = useState('');
  const [today, setToday] = useState('');
  const [nextDay, setNextDay] = useState('');
  const [nextDay1, setNextDay1] = useState('');
  const [nextDay2, setNextDay2] = useState('');
  const [nextDay3, setNextDay3] = useState('');
  const [nextDay4, setNextDay4] = useState('');
  const [nextDay5, setNextDay5] = useState('');
  const [continueView, setContinueView] = useState(true);
  
  useEffect(() => {
    if(dataAllClients === ''){
      clientAPI.getDataALLUsers().then(result => {
        const dateToday = moment().add(0, 'days').format('').slice(0, 10);
        const dateNextDay = moment().add(1, 'days').format('').slice(0, 10);
        const dateNextDay1 = moment().add(2, 'days').format('').slice(0, 10);
        const dateNextDay2 = moment().add(3, 'days').format('').slice(0, 10);
        const dateNextDay3 = moment().add(4, 'days').format('').slice(0, 10);
        const dateNextDay4 = moment().add(5, 'days').format('').slice(0, 10);
        const dateNextDay5 = moment().add(6, 'days').format('').slice(0, 10);

        const dayOfWeekToday = moment().add(0, 'days').format('dddd');
        const dayOfWeekNextDay = moment().add(1, 'days').format('dddd');
        const dayOfWeekNextDay1 = moment().add(2, 'days').format('dddd');
        const dayOfWeekNextDay2 = moment().add(3, 'days').format('dddd');
        const dayOfWeekNextDay3 = moment().add(4, 'days').format('dddd');
        const dayOfWeekNextDay4 = moment().add(5, 'days').format('dddd');
        const dayOfWeekNextDay5 = moment().add(6, 'days').format('dddd');

// ========================TODAY=============================
        const dataClientToday0800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '08:00'));
        const dataClientToday0900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '09:00'));
        const dataClientToday1000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '10:00'));
        const dataClientToday1100 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '11:00'));
        const dataClientToday1200 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '12:00'));
        const dataClientToday1300 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '13:00'));
        const dataClientToday1400 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '14:00'));
        const dataClientToday1500 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '15:00'));
        const dataClientToday1600 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '16:00'));
        const dataClientToday1700 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '17:00'));
        const dataClientToday1800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '18:00'));
        const dataClientToday1900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '19:00'));
        const dataClientToday2000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateToday && infoDate.time === '20:00'));
        if (dataClientToday0800.length === '') {
            return (setClientsToday0800(''));
          };
          if (dataClientToday0800.length === 1) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateToday});
            const clientBase = [client1]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 2) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 3) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 4) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client4 = dataClientToday0800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 5) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client4 = dataClientToday0800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client5 = dataClientToday0800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 6) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client4 = dataClientToday0800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client5 = dataClientToday0800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client6 = dataClientToday0800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 7) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client4 = dataClientToday0800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client5 = dataClientToday0800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client6 = dataClientToday0800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client7 = dataClientToday0800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 8) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client4 = dataClientToday0800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client5 = dataClientToday0800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client6 = dataClientToday0800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client7 = dataClientToday0800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client8 = dataClientToday0800[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 9) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client4 = dataClientToday0800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client5 = dataClientToday0800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client6 = dataClientToday0800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client7 = dataClientToday0800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client8 = dataClientToday0800[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client9 = dataClientToday0800[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday0800(clientBase);
          };
          if (dataClientToday0800.length === 10) {
            const client1 = dataClientToday0800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client2 = dataClientToday0800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client3 = dataClientToday0800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client4 = dataClientToday0800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client5 = dataClientToday0800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client6 = dataClientToday0800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client7 = dataClientToday0800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client8 = dataClientToday0800[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client9 = dataClientToday0800[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const client10 = dataClientToday0800[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '08:00'; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday0800(clientBase);
        };
        if (dataClientToday0900.length === '') {
            return (setClientsToday0900(''));
          };
          if (dataClientToday0900.length === 1) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 2) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 3) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 4) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday0900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 5) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday0900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday0900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 6) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday0900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday0900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday0900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 7) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday0900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday0900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday0900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday0900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 8) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday0900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday0900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday0900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday0900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday0900[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 9) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday0900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday0900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday0900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday0900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday0900[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday0900[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday0900(clientBase);
          };
          if (dataClientToday0900.length === 10) {
            const client1 = dataClientToday0900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday0900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday0900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday0900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday0900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday0900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday0900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday0900[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday0900[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday0900[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday0900(clientBase);
        };
        if (dataClientToday1000.length === '') {
            return (setClientsToday1000(''));
          };
          if (dataClientToday1000.length === 1) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 2) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 3) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 4) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 5) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 6) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 7) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 8) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1000[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 9) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1000[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1000[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1000(clientBase);
          };
          if (dataClientToday1000.length === 10) {
            const client1 = dataClientToday1000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1000[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1000[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1000[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1000(clientBase);
        };
        if (dataClientToday1100.length === '') {
            return (setClientsToday1100(''));
          };
          if (dataClientToday1100.length === 1) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 2) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 3) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 4) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1100[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 5) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1100[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1100[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 6) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1100[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1100[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1100[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 7) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1100[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1100[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1100[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1100[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 8) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1100[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1100[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1100[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1100[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1100[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 9) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1100[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1100[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1100[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1100[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1100[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1100[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1100(clientBase);
          };
          if (dataClientToday1100.length === 10) {
            const client1 = dataClientToday1100[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1100[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1100[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1100[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1100[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1100[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1100[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1100[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1100[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1100[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1100(clientBase);
        };
        if (dataClientToday1200.length === '') {
            return (setClientsToday1200(''));
          };
          if (dataClientToday1200.length === 1) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 2) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 3) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 4) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1200[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 5) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1200[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1200[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 6) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1200[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1200[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1200[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 7) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1200[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1200[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1200[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1200[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 8) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1200[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1200[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1200[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1200[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1200[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 9) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1200[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1200[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1200[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1200[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1200[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1200[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1200(clientBase);
          };
          if (dataClientToday1200.length === 10) {
            const client1 = dataClientToday1200[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1200[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1200[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1200[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1200[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1200[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1200[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1200[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1200[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1200[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1200(clientBase);
        };
        if (dataClientToday1300.length === '') {
            return (setClientsToday1300(''));
          };
          if (dataClientToday1300.length === 1) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 2) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 3) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 4) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1300[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 5) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1300[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1300[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 6) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1300[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1300[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1300[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 7) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1300[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1300[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1300[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1300[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 8) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1300[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1300[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1300[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1300[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1300[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 9) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1300[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1300[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1300[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1300[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1300[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1300[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1300(clientBase);
          };
          if (dataClientToday1300.length === 10) {
            const client1 = dataClientToday1300[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1300[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1300[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1300[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1300[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1300[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1300[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1300[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1300[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1300[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1300(clientBase);
        };
        if (dataClientToday1400.length === '') {
          return (setClientsToday1400(''));
        };
        if (dataClientToday1400.length === 1) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 2) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 3) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 4) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client4 = dataClientToday1400[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3, client4]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 5) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client4 = dataClientToday1400[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client5 = dataClientToday1400[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3, client4, client5]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 6) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client4 = dataClientToday1400[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client5 = dataClientToday1400[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client6 = dataClientToday1400[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3, client4, client5, client6]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 7) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client4 = dataClientToday1400[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client5 = dataClientToday1400[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client6 = dataClientToday1400[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client7 = dataClientToday1400[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 8) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client4 = dataClientToday1400[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client5 = dataClientToday1400[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client6 = dataClientToday1400[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client7 = dataClientToday1400[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client8 = dataClientToday1400[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 9) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client4 = dataClientToday1400[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client5 = dataClientToday1400[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client6 = dataClientToday1400[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client7 = dataClientToday1400[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client8 = dataClientToday1400[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client9 = dataClientToday1400[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1400.length === 10) {
          const client1 = dataClientToday1400[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client2 = dataClientToday1400[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client3 = dataClientToday1400[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client4 = dataClientToday1400[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client5 = dataClientToday1400[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client6 = dataClientToday1400[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client7 = dataClientToday1400[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client8 = dataClientToday1400[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client9 = dataClientToday1400[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const client10 = dataClientToday1400[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateToday; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
          setClientsToday1400(clientBase);
        };
        if (dataClientToday1500.length === '') {
            return (setClientsToday1500(''));
          };
          if (dataClientToday1500.length === 1) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 2) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 3) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 4) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1500[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 5) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1500[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1500[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 6) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1500[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1500[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1500[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 7) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1500[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1500[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1500[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1500[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 8) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1500[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1500[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1500[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1500[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1500[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 9) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1500[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1500[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1500[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1500[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1500[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1500[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1500(clientBase);
          };
          if (dataClientToday1500.length === 10) {
            const client1 = dataClientToday1500[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1500[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1500[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1500[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1500[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1500[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1500[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1500[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1500[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1500[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1500(clientBase);
        };
        if (dataClientToday1600.length === '') {
            return (setClientsToday1600(''));
          };
          if (dataClientToday1600.length === 1) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 2) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 3) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 4) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1600[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 5) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1600[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1600[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 6) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1600[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1600[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1600[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 7) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1600[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1600[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1600[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1600[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 8) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1600[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1600[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1600[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1600[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1600[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 9) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1600[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1600[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1600[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1600[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1600[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1600[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1600(clientBase);
          };
          if (dataClientToday1600.length === 10) {
            const client1 = dataClientToday1600[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1600[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1600[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1600[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1600[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1600[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1600[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1600[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1600[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1600[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1600(clientBase);
        };
        if (dataClientToday1700.length === '') {
            return (setClientsToday1700(''));
          };
          if (dataClientToday1700.length === 1) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 2) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 3) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 4) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1700[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 5) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1700[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1700[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 6) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1700[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1700[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1700[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 7) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1700[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1700[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1700[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1700[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 8) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1700[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1700[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1700[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1700[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1700[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 9) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1700[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1700[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1700[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1700[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1700[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1700[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1700(clientBase);
          };
          if (dataClientToday1700.length === 10) {
            const client1 = dataClientToday1700[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1700[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1700[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1700[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1700[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1700[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1700[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1700[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1700[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1700[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1700(clientBase);
        };
        if (dataClientToday1800.length === '') {
            return (setClientsToday1800(''));
          };
          if (dataClientToday1800.length === 1) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 2) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 3) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 4) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 5) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 6) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 7) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 8) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1800[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 9) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1800[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1800[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1800(clientBase);
          };
          if (dataClientToday1800.length === 10) {
            const client1 = dataClientToday1800[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1800[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1800[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1800[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1800[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1800[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1800[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1800[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1800[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1800[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1800(clientBase);
        };
        if (dataClientToday1900.length === '') {
          return (setClientsToday1900(''));
          };
          if (dataClientToday1900.length === 1) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 2) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 3) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 4) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 5) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 6) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 7) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 8) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1900[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 9) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1900[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1900[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday1900(clientBase);
          };
          if (dataClientToday1900.length === 10) {
            const client1 = dataClientToday1900[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday1900[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday1900[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday1900[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday1900[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday1900[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday1900[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday1900[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday1900[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday1900[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday1900(clientBase);
        };
        if (dataClientToday2000.length === '') {
            return (setClientsToday2000(''));
          };
          if (dataClientToday2000.length === 1) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 2) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 3) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 4) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday2000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 5) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday2000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday2000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 6) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday2000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday2000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday2000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 7) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday2000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday2000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday2000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday2000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 8) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday2000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday2000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday2000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday2000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday2000[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 9) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday2000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday2000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday2000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday2000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday2000[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday2000[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsToday2000(clientBase);
          };
          if (dataClientToday2000.length === 10) {
            const client1 = dataClientToday2000[0].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client2 = dataClientToday2000[1].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client3 = dataClientToday2000[2].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client4 = dataClientToday2000[3].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client5 = dataClientToday2000[4].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client6 = dataClientToday2000[5].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client7 = dataClientToday2000[6].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client8 = dataClientToday2000[7].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client9 = dataClientToday2000[8].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const client10 = dataClientToday2000[9].info.find((value) => { return value.day === dayOfWeekToday.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateToday; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsToday2000(clientBase);
        };
// =========================NextDay================================================================================================
        const dataClientNextDay0800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '08:00'));
        const dataClientNextDay0900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '09:00'));
        const dataClientNextDay1000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '10:00'));
        const dataClientNextDay1100 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '11:00'));
        const dataClientNextDay1200 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '12:00'));
        const dataClientNextDay1300 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '13:00'));
        const dataClientNextDay1400 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '14:00'));
        const dataClientNextDay1500 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '15:00'));
        const dataClientNextDay1600 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '16:00'));
        const dataClientNextDay1700 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '17:00'));
        const dataClientNextDay1800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '18:00'));
        const dataClientNextDay1900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '19:00'));
        const dataClientNextDay2000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay && infoDate.time === '20:00'));
        // console.log(dataClientNextDay0900)
        if (dataClientNextDay0800.length === '') {
            return (setClientsNextDay0800(''));
          };
          if (dataClientNextDay0800.length === 1) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 2) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 3) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 4) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 5) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 6) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 7) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 8) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay0800[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 9) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay0800[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay0800[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay0800(clientBase);
          };
          if (dataClientNextDay0800.length === 10) {
            const client1 = dataClientNextDay0800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay0800[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay0800[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay0800[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay0800(clientBase);
        };
        if (dataClientNextDay0900.length === '') {
            return (setClientsNextDay0900(''));
          };
          // console.log("dataClientNextDay0900: ", dataClientNextDay0900)
          if (dataClientNextDay0900.length === 1) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            // console.log(client1)
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 2) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 3) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 4) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 5) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 6) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 7) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 8) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay0900[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 9) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay0900[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay0900[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay0900.length === 10) {
            const client1 = dataClientNextDay0900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay0900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay0900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay0900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay0900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay0900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay0900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay0900[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay0900[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay0900[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay0900(clientBase);
          };
          if (dataClientNextDay1000.length === '') {
            return (setClientsNextDay1000(''));
          };
          if (dataClientNextDay1000.length === 1) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 2) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 3) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 4) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 5) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 6) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 7) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 8) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1000[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 9) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1000[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1000[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1000.length === 10) {
            const client1 = dataClientNextDay1000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1000[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1000[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1000[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1000(clientBase);
          };
          if (dataClientNextDay1100.length === '') {
            return (setClientsNextDay1100(''));
          };
          if (dataClientNextDay1100.length === 1) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 2) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 3) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 4) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1100[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 5) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1100[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1100[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 6) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1100[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1100[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1100[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 7) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1100[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1100[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1100[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1100[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 8) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1100[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1100[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1100[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1100[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1100[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 9) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1100[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1100[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1100[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1100[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1100[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1100[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1100.length === 10) {
            const client1 = dataClientNextDay1100[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1100[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1100[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1100[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1100[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1100[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1100[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1100[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1100[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1100[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1100(clientBase);
          };
          if (dataClientNextDay1200.length === '') {
            return (setClientsNextDay1200(''));
          };
          if (dataClientNextDay1200.length === 1) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 2) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 3) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 4) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1200[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 5) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1200[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1200[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 6) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1200[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1200[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1200[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 7) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1200[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1200[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1200[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1200[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 8) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1200[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1200[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1200[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1200[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1200[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 9) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1200[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1200[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1200[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1200[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1200[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1200[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1200.length === 10) {
            const client1 = dataClientNextDay1200[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1200[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1200[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1200[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1200[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1200[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1200[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1200[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1200[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1200[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1200(clientBase);
          };
          if (dataClientNextDay1300.length === '') {
            return (setClientsNextDay1300(''));
          };
          if (dataClientNextDay1300.length === 1) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 2) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 3) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 4) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1300[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 5) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1300[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1300[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 6) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1300[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1300[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1300[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 7) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1300[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1300[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1300[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1300[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 8) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1300[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1300[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1300[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1300[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1300[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 9) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1300[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1300[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1300[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1300[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1300[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1300[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1300.length === 10) {
            const client1 = dataClientNextDay1300[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1300[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1300[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1300[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1300[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1300[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1300[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1300[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1300[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1300[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1300(clientBase);
          };
          if (dataClientNextDay1400.length === '') {
          return (setClientsNextDay1400(''));
          };
          if (dataClientNextDay1400.length === 1) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 2) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 3) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 4) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client4 = dataClientNextDay1400[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3, client4]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 5) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client4 = dataClientNextDay1400[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client5 = dataClientNextDay1400[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3, client4, client5]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 6) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client4 = dataClientNextDay1400[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client5 = dataClientNextDay1400[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client6 = dataClientNextDay1400[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3, client4, client5, client6]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 7) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client4 = dataClientNextDay1400[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client5 = dataClientNextDay1400[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client6 = dataClientNextDay1400[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client7 = dataClientNextDay1400[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 8) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client4 = dataClientNextDay1400[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client5 = dataClientNextDay1400[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client6 = dataClientNextDay1400[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client7 = dataClientNextDay1400[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client8 = dataClientNextDay1400[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 9) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client4 = dataClientNextDay1400[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client5 = dataClientNextDay1400[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client6 = dataClientNextDay1400[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client7 = dataClientNextDay1400[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client8 = dataClientNextDay1400[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client9 = dataClientNextDay1400[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1400.length === 10) {
          const client1 = dataClientNextDay1400[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client2 = dataClientNextDay1400[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client3 = dataClientNextDay1400[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client4 = dataClientNextDay1400[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client5 = dataClientNextDay1400[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client6 = dataClientNextDay1400[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client7 = dataClientNextDay1400[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client8 = dataClientNextDay1400[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client9 = dataClientNextDay1400[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const client10 = dataClientNextDay1400[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
          setClientsNextDay1400(clientBase);
          };
          if (dataClientNextDay1500.length === '') {
            return (setClientsNextDay1500(''));
          };
          if (dataClientNextDay1500.length === 1) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 2) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 3) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 4) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1500[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 5) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1500[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1500[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 6) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1500[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1500[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1500[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 7) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1500[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1500[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1500[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1500[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 8) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1500[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1500[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1500[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1500[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1500[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 9) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1500[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1500[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1500[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1500[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1500[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1500[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1500.length === 10) {
            const client1 = dataClientNextDay1500[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1500[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1500[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1500[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1500[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1500[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1500[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1500[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1500[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1500[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1500(clientBase);
          };
          if (dataClientNextDay1600.length === '') {
            return (setClientsNextDay1600(''));
          };
          if (dataClientNextDay1600.length === 1) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 2) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 3) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 4) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1600[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 5) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1600[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1600[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 6) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1600[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1600[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1600[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 7) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1600[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1600[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1600[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1600[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 8) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1600[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1600[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1600[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1600[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1600[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 9) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1600[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1600[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1600[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1600[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1600[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1600[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1600.length === 10) {
            const client1 = dataClientNextDay1600[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1600[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1600[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1600[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1600[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1600[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1600[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1600[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1600[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1600[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1600(clientBase);
          };
          if (dataClientNextDay1700.length === '') {
            return (setClientsNextDay1700(''));
          };
          if (dataClientNextDay1700.length === 1) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 2) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 3) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 4) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1700[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 5) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1700[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1700[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 6) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1700[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1700[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1700[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 7) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1700[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1700[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1700[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1700[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 8) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1700[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1700[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1700[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1700[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1700[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 9) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1700[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1700[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1700[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1700[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1700[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1700[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1700.length === 10) {
            const client1 = dataClientNextDay1700[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1700[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1700[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1700[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1700[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1700[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1700[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1700[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1700[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1700[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1700(clientBase);
          };
          if (dataClientNextDay1800.length === '') {
            return (setClientsNextDay1800(''));
          };
          if (dataClientNextDay1800.length === 1) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 2) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 3) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 4) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 5) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 6) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 7) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 8) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1800[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 9) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1800[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1800[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1800.length === 10) {
            const client1 = dataClientNextDay1800[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1800[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1800[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1800[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1800[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1800[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1800[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1800[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1800[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1800[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1800(clientBase);
          };
          if (dataClientNextDay1900.length === '') {
          return (setClientsNextDay1900(''));
          };
          if (dataClientNextDay1900.length === 1) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 2) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 3) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 4) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 5) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 6) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 7) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 8) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1900[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 9) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1900[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1900[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay1900.length === 10) {
            const client1 = dataClientNextDay1900[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay1900[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay1900[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay1900[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay1900[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay1900[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay1900[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay1900[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay1900[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay1900[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay1900(clientBase);
          };
          if (dataClientNextDay2000.length === '') {
            return (setClientsNextDay2000(''));
          };
          if (dataClientNextDay2000.length === 1) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 2) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 3) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 4) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay2000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 5) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay2000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay2000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 6) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay2000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay2000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay2000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 7) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay2000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay2000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay2000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay2000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 8) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay2000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay2000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay2000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay2000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay2000[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 9) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay2000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay2000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay2000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay2000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay2000[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay2000[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDay2000(clientBase);
          };
          if (dataClientNextDay2000.length === 10) {
            const client1 = dataClientNextDay2000[0].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client2 = dataClientNextDay2000[1].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client3 = dataClientNextDay2000[2].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client4 = dataClientNextDay2000[3].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client5 = dataClientNextDay2000[4].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client6 = dataClientNextDay2000[5].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client7 = dataClientNextDay2000[6].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client8 = dataClientNextDay2000[7].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client9 = dataClientNextDay2000[8].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const client10 = dataClientNextDay2000[9].info.find((value) => { return value.day === dayOfWeekNextDay.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay && value.date.slice(0, 10) === dateNextDay; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDay2000(clientBase);
          };
// =========================NextDayTwo================================================================================================
        const dataClientNextDayTwo0800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '08:00'));
        const dataClientNextDayTwo0900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '09:00'));
        const dataClientNextDayTwo1000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '10:00'));
        const dataClientNextDayTwo1100 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '11:00'));
        const dataClientNextDayTwo1200 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '12:00'));
        const dataClientNextDayTwo1300 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '13:00'));
        const dataClientNextDayTwo1400 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '14:00'));
        const dataClientNextDayTwo1500 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '15:00'));
        const dataClientNextDayTwo1600 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '16:00'));
        const dataClientNextDayTwo1700 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '17:00'));
        const dataClientNextDayTwo1800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '18:00'));
        const dataClientNextDayTwo1900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '19:00'));
        const dataClientNextDayTwo2000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay1 && infoDate.time === '20:00'));
        if (dataClientNextDayTwo0800.length === '') {
            return (setClientsNextDayTwo0800(''));
          };
          if (dataClientNextDayTwo0800.length === 1) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 2) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 3) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 4) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 5) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 6) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1;});
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 7) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 8) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo0800[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 9) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo0800[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo0800[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0800.length === 10) {
            const client1 = dataClientNextDayTwo0800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo0800[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo0800[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo0800[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo0800(clientBase);
          };
          if (dataClientNextDayTwo0900.length === '') {
            return (setClientsNextDayTwo0900(''));
          };
          if (dataClientNextDayTwo0900.length === 1) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 2) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 3) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 4) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 5) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 6) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 7) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 8) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo0900[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 9) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo0900[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo0900[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo0900.length === 10) {
            const client1 = dataClientNextDayTwo0900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo0900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo0900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo0900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo0900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo0900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo0900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo0900[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo0900[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo0900[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo0900(clientBase);
          };
          if (dataClientNextDayTwo1000.length === '') {
            return (setClientsNextDayTwo1000(''));
          };
          if (dataClientNextDayTwo1000.length === 1) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 2) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 3) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 4) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 5) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 6) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 7) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 8) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1000[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 9) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1000[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1000[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1000.length === 10) {
            const client1 = dataClientNextDayTwo1000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1000[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1000[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1000[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1000(clientBase);
          };
          if (dataClientNextDayTwo1100.length === '') {
            return (setClientsNextDayTwo1100(''));
          };
          if (dataClientNextDayTwo1100.length === 1) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 2) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 3) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 4) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1100[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 5) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1100[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1100[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 6) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1100[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1100[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1100[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 7) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1100[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1100[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1100[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1100[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 8) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1100[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1100[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1100[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1100[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1100[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 9) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1100[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1100[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1100[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1100[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1100[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1100[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1100.length === 10) {
            const client1 = dataClientNextDayTwo1100[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1100[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1100[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1100[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1100[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1100[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1100[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1100[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1100[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1100[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1100(clientBase);
          };
          if (dataClientNextDayTwo1200.length === '') {
            return (setClientsNextDayTwo1200(''));
          };
          if (dataClientNextDayTwo1200.length === 1) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 2) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 3) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 4) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1200[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 5) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1200[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1200[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 6) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1200[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1200[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1200[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 7) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1200[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1200[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1200[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1200[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 8) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1200[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1200[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1200[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1200[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1200[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 9) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1200[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1200[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1200[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1200[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1200[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1200[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1200.length === 10) {
            const client1 = dataClientNextDayTwo1200[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1200[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1200[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1200[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1200[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1200[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1200[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1200[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1200[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1200[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1200(clientBase);
          };
          if (dataClientNextDayTwo1300.length === '') {
            return (setClientsNextDayTwo1300(''));
          };
          if (dataClientNextDayTwo1300.length === 1) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 2) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 3) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 4) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1300[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 5) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1300[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1300[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 6) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1300[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1300[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1300[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 7) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1300[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1300[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1300[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1300[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 8) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1300[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1300[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1300[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1300[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1300[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 9) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1300[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1300[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1300[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1300[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1300[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1300[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1300(clientBase);
          };
          if (dataClientNextDayTwo1300.length === 10) {
            const client1 = dataClientNextDayTwo1300[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1300[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1300[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1300[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1300[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1300[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1300[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1300[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1300[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1300[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1300(clientBase);
          };
          // console.log("dataClientNextDayTwo1400: ", dataClientNextDayTwo1400)
          // console.log("dataClientNextDayTwo1400: ", dataClientNextDayTwo1400.length)
          // console.log("dayOfWeekToday: ", dayOfWeekToday)
          // console.log("dayOfWeekNextDay: ", dayOfWeekNextDay)
          // console.log("dayOfWeekNextDay1: ", dayOfWeekNextDay1)
          // console.log("dayOfWeekNextDay1: ", dayOfWeekNextDay1)
          if (dataClientNextDayTwo1400.length === '') {
          return (setClientsNextDayTwo1400(''));
          };
          if (dataClientNextDayTwo1400.length === 1) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1]
          console.log(clientBase)
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 2) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 3) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 4) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client4 = dataClientNextDayTwo1400[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3, client4]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 5) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client4 = dataClientNextDayTwo1400[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client5 = dataClientNextDayTwo1400[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3, client4, client5]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 6) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client4 = dataClientNextDayTwo1400[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client5 = dataClientNextDayTwo1400[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client6 = dataClientNextDayTwo1400[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3, client4, client5, client6]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 7) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client4 = dataClientNextDayTwo1400[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client5 = dataClientNextDayTwo1400[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client6 = dataClientNextDayTwo1400[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client7 = dataClientNextDayTwo1400[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 8) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client4 = dataClientNextDayTwo1400[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client5 = dataClientNextDayTwo1400[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client6 = dataClientNextDayTwo1400[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client7 = dataClientNextDayTwo1400[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client8 = dataClientNextDayTwo1400[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 9) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client4 = dataClientNextDayTwo1400[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client5 = dataClientNextDayTwo1400[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client6 = dataClientNextDayTwo1400[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client7 = dataClientNextDayTwo1400[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client8 = dataClientNextDayTwo1400[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client9 = dataClientNextDayTwo1400[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1400.length === 10) {
          const client1 = dataClientNextDayTwo1400[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client2 = dataClientNextDayTwo1400[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client3 = dataClientNextDayTwo1400[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client4 = dataClientNextDayTwo1400[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client5 = dataClientNextDayTwo1400[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client6 = dataClientNextDayTwo1400[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client7 = dataClientNextDayTwo1400[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client8 = dataClientNextDayTwo1400[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client9 = dataClientNextDayTwo1400[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const client10 = dataClientNextDayTwo1400[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay1; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
          setClientsNextDayTwo1400(clientBase);
          };
          if (dataClientNextDayTwo1500.length === '') {
            return (setClientsNextDayTwo1500(''));
          };
          if (dataClientNextDayTwo1500.length === 1) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 2) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 3) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 4) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1500[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 5) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1500[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1500[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 6) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1500[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1500[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1500[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 7) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1500[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1500[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1500[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1500[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 8) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1500[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1500[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1500[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1500[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1500[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 9) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1500[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1500[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1500[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1500[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1500[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1500[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1500.length === 10) {
            const client1 = dataClientNextDayTwo1500[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1500[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1500[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1500[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1500[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1500[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1500[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1500[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1500[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1500[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1500(clientBase);
          };
          if (dataClientNextDayTwo1600.length === '') {
            return (setClientsNextDayTwo1600(''));
          };
          if (dataClientNextDayTwo1600.length === 1) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 2) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 3) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 4) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1600[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 5) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1600[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1600[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 6) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1600[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1600[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1600[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 7) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1600[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1600[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1600[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1600[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 8) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1600[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1600[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1600[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1600[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1600[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 9) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1600[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1600[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1600[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1600[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1600[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1600[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1600.length === 10) {
            const client1 = dataClientNextDayTwo1600[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1600[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1600[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1600[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1600[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1600[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1600[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1600[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1600[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1600[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1600(clientBase);
          };
          if (dataClientNextDayTwo1700.length === '') {
            return (setClientsNextDayTwo1700(''));
          };
          if (dataClientNextDayTwo1700.length === 1) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 2) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 3) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 4) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1700[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 5) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1700[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1700[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 6) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1700[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1700[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1700[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 7) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1700[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1700[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1700[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1700[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 8) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1700[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1700[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1700[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1700[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1700[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 9) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1700[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1700[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1700[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1700[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1700[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1700[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1700.length === 10) {
            const client1 = dataClientNextDayTwo1700[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1700[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1700[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1700[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1700[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1700[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1700[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1700[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1700[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1700[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1700(clientBase);
          };
          if (dataClientNextDayTwo1800.length === '') {
            return (setClientsNextDayTwo1800(''));
          };
          if (dataClientNextDayTwo1800.length === 1) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 2) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 3) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 4) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 5) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 6) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 7) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 8) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1800[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 9) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1800[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1800[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1800.length === 10) {
            const client1 = dataClientNextDayTwo1800[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1800[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1800[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1800[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1800[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1800[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1800[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1800[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1800[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1800[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1800(clientBase);
          };
          if (dataClientNextDayTwo1900.length === '') {
          return (setClientsNextDayTwo1900(''));
          };
          if (dataClientNextDayTwo1900.length === 1) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 2) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 3) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 4) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 5) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 6) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 7) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 8) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1900[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 9) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1900[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1900[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo1900.length === 10) {
            const client1 = dataClientNextDayTwo1900[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo1900[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo1900[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo1900[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo1900[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo1900[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo1900[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo1900[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo1900[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo1900[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo1900(clientBase);
          };
          if (dataClientNextDayTwo2000.length === '') {
            return (setClientsNextDayTwo2000(''));
          };
          if (dataClientNextDayTwo2000.length === 1) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 2) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 3) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 4) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo2000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 5) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo2000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo2000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 6) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo2000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo2000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo2000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 7) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo2000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo2000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo2000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo2000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 8) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo2000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo2000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo2000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo2000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo2000[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 9) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo2000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo2000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo2000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo2000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo2000[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo2000[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayTwo2000(clientBase);
          };
          if (dataClientNextDayTwo2000.length === 10) {
            const client1 = dataClientNextDayTwo2000[0].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client2 = dataClientNextDayTwo2000[1].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client3 = dataClientNextDayTwo2000[2].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client4 = dataClientNextDayTwo2000[3].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client5 = dataClientNextDayTwo2000[4].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client6 = dataClientNextDayTwo2000[5].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client7 = dataClientNextDayTwo2000[6].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client8 = dataClientNextDayTwo2000[7].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client9 = dataClientNextDayTwo2000[8].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const client10 = dataClientNextDayTwo2000[9].info.find((value) => { return value.day === dayOfWeekNextDay1.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay1; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayTwo2000(clientBase);
          };
// =========================NextDayThree================================================================================================
        const dataClientNextDayThree0800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '08:00'));
        const dataClientNextDayThree0900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '09:00'));
        const dataClientNextDayThree1000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '10:00'));
        const dataClientNextDayThree1100 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '11:00'));
        const dataClientNextDayThree1200 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '12:00'));
        const dataClientNextDayThree1300 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '13:00'));
        const dataClientNextDayThree1400 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '14:00'));
        const dataClientNextDayThree1500 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '15:00'));
        const dataClientNextDayThree1600 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '16:00'));
        const dataClientNextDayThree1700 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '17:00'));
        const dataClientNextDayThree1800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '18:00'));
        const dataClientNextDayThree1900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '19:00'));
        const dataClientNextDayThree2000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay2 && infoDate.time === '20:00'));
        if (dataClientNextDayThree0800.length === '') {
            return (setClientsNextDayThree0800(''));
          };
          if (dataClientNextDayThree0800.length === 1) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 2) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 3) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 4) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 5) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 6) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 7) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 8) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree0800[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 9) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree0800[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree0800[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree0800(clientBase);
          };
          if (dataClientNextDayThree0800.length === 10) {
            const client1 = dataClientNextDayThree0800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree0800[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree0800[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree0800[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree0800(clientBase);
        };
        if (dataClientNextDayThree0900.length === '') {
            return (setClientsNextDayThree0900(''));
          };
          if (dataClientNextDayThree0900.length === 1) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 2) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 3) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 4) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 5) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 6) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 7) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 8) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree0900[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 9) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree0900[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree0900[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree0900.length === 10) {
            const client1 = dataClientNextDayThree0900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree0900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree0900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree0900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree0900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree0900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree0900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree0900[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree0900[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree0900[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree0900(clientBase);
          };
          if (dataClientNextDayThree1000.length === '') {
            return (setClientsNextDayThree1000(''));
          };
          if (dataClientNextDayThree1000.length === 1) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 2) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 3) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 4) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 5) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 6) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 7) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 8) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1000[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 9) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1000[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1000[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1000.length === 10) {
            const client1 = dataClientNextDayThree1000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1000[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1000[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1000[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1000(clientBase);
          };
          if (dataClientNextDayThree1100.length === '') {
            return (setClientsNextDayThree1100(''));
          };
          if (dataClientNextDayThree1100.length === 1) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 2) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 3) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 4) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1100[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 5) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1100[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1100[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 6) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1100[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1100[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1100[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 7) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1100[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1100[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1100[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1100[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 8) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1100[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1100[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1100[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1100[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1100[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 9) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1100[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1100[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1100[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1100[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1100[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1100[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1100.length === 10) {
            const client1 = dataClientNextDayThree1100[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1100[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1100[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1100[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1100[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1100[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1100[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1100[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1100[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1100[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1100(clientBase);
          };
          if (dataClientNextDayThree1200.length === '') {
            return (setClientsNextDayThree1200(''));
          };
          if (dataClientNextDayThree1200.length === 1) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 2) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 3) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 4) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1200[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 5) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1200[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1200[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 6) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1200[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1200[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1200[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 7) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1200[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1200[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1200[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1200[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 8) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1200[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1200[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1200[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1200[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1200[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 9) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1200[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1200[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1200[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1200[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1200[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1200[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1200.length === 10) {
            const client1 = dataClientNextDayThree1200[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1200[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1200[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1200[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1200[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1200[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1200[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1200[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1200[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1200[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1200(clientBase);
          };
          if (dataClientNextDayThree1300.length === '') {
            return (setClientsNextDayThree1300(''));
          };
          if (dataClientNextDayThree1300.length === 1) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 2) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 3) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 4) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1300[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 5) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1300[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1300[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 6) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1300[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1300[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1300[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 7) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1300[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1300[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1300[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1300[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 8) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1300[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1300[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1300[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1300[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1300[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 9) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1300[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1300[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1300[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1300[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1300[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1300[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1300.length === 10) {
            const client1 = dataClientNextDayThree1300[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1300[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1300[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1300[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1300[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1300[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1300[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1300[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1300[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1300[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1300(clientBase);
          };
          if (dataClientNextDayThree1400.length === '') {
          return (setClientsNextDayThree1400(''));
          };
          if (dataClientNextDayThree1400.length === 1) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 2) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 3) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 4) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client4 = dataClientNextDayThree1400[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3, client4]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 5) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client4 = dataClientNextDayThree1400[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client5 = dataClientNextDayThree1400[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3, client4, client5]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 6) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client4 = dataClientNextDayThree1400[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client5 = dataClientNextDayThree1400[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client6 = dataClientNextDayThree1400[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3, client4, client5, client6]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 7) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client4 = dataClientNextDayThree1400[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client5 = dataClientNextDayThree1400[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client6 = dataClientNextDayThree1400[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client7 = dataClientNextDayThree1400[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 8) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client4 = dataClientNextDayThree1400[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client5 = dataClientNextDayThree1400[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client6 = dataClientNextDayThree1400[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client7 = dataClientNextDayThree1400[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client8 = dataClientNextDayThree1400[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 9) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client4 = dataClientNextDayThree1400[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client5 = dataClientNextDayThree1400[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client6 = dataClientNextDayThree1400[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client7 = dataClientNextDayThree1400[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client8 = dataClientNextDayThree1400[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client9 = dataClientNextDayThree1400[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1400.length === 10) {
          const client1 = dataClientNextDayThree1400[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client2 = dataClientNextDayThree1400[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client3 = dataClientNextDayThree1400[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client4 = dataClientNextDayThree1400[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client5 = dataClientNextDayThree1400[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client6 = dataClientNextDayThree1400[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client7 = dataClientNextDayThree1400[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client8 = dataClientNextDayThree1400[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client9 = dataClientNextDayThree1400[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const client10 = dataClientNextDayThree1400[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay2; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
          setClientsNextDayThree1400(clientBase);
          };
          if (dataClientNextDayThree1500.length === '') {
            return (setClientsNextDayThree1500(''));
          };
          if (dataClientNextDayThree1500.length === 1) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 2) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 3) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 4) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1500[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 5) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1500[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1500[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 6) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1500[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1500[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1500[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 7) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1500[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1500[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1500[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1500[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 8) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1500[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1500[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1500[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1500[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1500[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 9) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1500[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1500[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1500[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1500[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1500[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1500[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1500.length === 10) {
            const client1 = dataClientNextDayThree1500[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1500[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1500[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1500[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1500[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1500[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1500[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1500[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1500[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1500[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1500(clientBase);
          };
          if (dataClientNextDayThree1600.length === '') {
            return (setClientsNextDayThree1600(''));
          };
          if (dataClientNextDayThree1600.length === 1) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 2) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 3) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 4) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1600[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 5) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1600[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1600[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 6) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1600[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1600[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1600[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 7) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1600[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1600[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1600[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1600[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 8) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1600[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1600[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1600[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1600[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1600[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 9) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1600[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1600[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1600[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1600[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1600[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1600[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1600.length === 10) {
            const client1 = dataClientNextDayThree1600[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1600[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1600[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1600[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1600[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1600[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1600[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1600[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1600[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1600[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1600(clientBase);
          };
          if (dataClientNextDayThree1700.length === '') {
            return (setClientsNextDayThree1700(''));
          };
          if (dataClientNextDayThree1700.length === 1) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 2) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 3) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 4) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1700[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 5) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1700[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1700[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 6) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1700[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1700[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1700[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 7) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1700[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1700[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1700[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1700[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 8) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1700[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1700[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1700[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1700[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1700[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 9) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1700[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1700[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1700[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1700[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1700[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1700[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1700.length === 10) {
            const client1 = dataClientNextDayThree1700[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1700[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1700[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1700[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1700[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1700[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1700[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1700[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1700[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1700[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1700(clientBase);
          };
          if (dataClientNextDayThree1800.length === '') {
            return (setClientsNextDayThree1800(''));
          };
          if (dataClientNextDayThree1800.length === 1) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 2) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 3) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 4) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 5) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 6) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 7) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 8) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1800[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 9) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1800[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1800[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1800.length === 10) {
            const client1 = dataClientNextDayThree1800[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1800[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1800[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1800[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1800[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1800[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1800[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1800[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1800[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1800[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1800(clientBase);
          };
          if (dataClientNextDayThree1900.length === '') {
          return (setClientsNextDayThree1900(''));
          };
          if (dataClientNextDayThree1900.length === 1) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 2) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 3) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 4) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 5) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 6) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 7) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 8) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1900[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 9) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1900[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1900[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree1900.length === 10) {
            const client1 = dataClientNextDayThree1900[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree1900[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree1900[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree1900[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree1900[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree1900[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree1900[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree1900[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree1900[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree1900[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree1900(clientBase);
          };
          if (dataClientNextDayThree2000.length === '') {
            return (setClientsNextDayThree2000(''));
          };
          if (dataClientNextDayThree2000.length === 1) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 2) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 3) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 4) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree2000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 5) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree2000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree2000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 6) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree2000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree2000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree2000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 7) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree2000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree2000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree2000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree2000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 8) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree2000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree2000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree2000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree2000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree2000[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 9) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree2000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree2000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree2000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree2000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree2000[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree2000[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayThree2000(clientBase);
          };
          if (dataClientNextDayThree2000.length === 10) {
            const client1 = dataClientNextDayThree2000[0].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client2 = dataClientNextDayThree2000[1].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client3 = dataClientNextDayThree2000[2].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client4 = dataClientNextDayThree2000[3].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client5 = dataClientNextDayThree2000[4].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client6 = dataClientNextDayThree2000[5].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client7 = dataClientNextDayThree2000[6].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client8 = dataClientNextDayThree2000[7].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client9 = dataClientNextDayThree2000[8].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const client10 = dataClientNextDayThree2000[9].info.find((value) => { return value.day === dayOfWeekNextDay2.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay2; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayThree2000(clientBase);
          };
// =========================NextDayFour================================================================================================
        const dataClientNextDayFour0800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '08:00'));
        const dataClientNextDayFour0900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '09:00'));
        const dataClientNextDayFour1000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '10:00'));
        const dataClientNextDayFour1100 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '11:00'));
        const dataClientNextDayFour1200 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '12:00'));
        const dataClientNextDayFour1300 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '13:00'));
        const dataClientNextDayFour1400 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '14:00'));
        const dataClientNextDayFour1500 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '15:00'));
        const dataClientNextDayFour1600 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '16:00'));
        const dataClientNextDayFour1700 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '17:00'));
        const dataClientNextDayFour1800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '18:00'));
        const dataClientNextDayFour1900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '19:00'));
        const dataClientNextDayFour2000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay3 && infoDate.time === '20:00'));
        if (dataClientNextDayFour0800.length === '') {
            return (setClientsNextDayFour0800(''));
          };
          if (dataClientNextDayFour0800.length === 1) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 2) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 3) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 4) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 5) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 6) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 7) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 8) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour0800[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 9) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour0800[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour0800[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour0800(clientBase);
          };
          if (dataClientNextDayFour0800.length === 10) {
            const client1 = dataClientNextDayFour0800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour0800[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour0800[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour0800[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour0800(clientBase);
        };
        if (dataClientNextDayFour0900.length === '') {
            return (setClientsNextDayFour0900(''));
          };
          if (dataClientNextDayFour0900.length === 1) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 2) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 3) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 4) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 5) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 6) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 7) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 8) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour0900[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 9) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour0900[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour0900[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour0900.length === 10) {
            const client1 = dataClientNextDayFour0900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour0900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour0900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour0900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour0900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour0900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour0900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour0900[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour0900[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour0900[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour0900(clientBase);
          };
          if (dataClientNextDayFour1000.length === '') {
            return (setClientsNextDayFour1000(''));
          };
          if (dataClientNextDayFour1000.length === 1) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 2) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 3) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 4) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 5) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 6) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 7) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 8) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1000[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 9) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1000[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1000[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1000.length === 10) {
            const client1 = dataClientNextDayFour1000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1000[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1000[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1000[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1000(clientBase);
          };
          if (dataClientNextDayFour1100.length === '') {
            return (setClientsNextDayFour1100(''));
          };
          if (dataClientNextDayFour1100.length === 1) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 2) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 3) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 4) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1100[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 5) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1100[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1100[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 6) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1100[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1100[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1100[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 7) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1100[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1100[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1100[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1100[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 8) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1100[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1100[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1100[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1100[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1100[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 9) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1100[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1100[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1100[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1100[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1100[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1100[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1100.length === 10) {
            const client1 = dataClientNextDayFour1100[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1100[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1100[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1100[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1100[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1100[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1100[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1100[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1100[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1100[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1100(clientBase);
          };
          if (dataClientNextDayFour1200.length === '') {
            return (setClientsNextDayFour1200(''));
          };
          if (dataClientNextDayFour1200.length === 1) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 2) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 3) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 4) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1200[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 5) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1200[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1200[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 6) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1200[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1200[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1200[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 7) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1200[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1200[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1200[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1200[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 8) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1200[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1200[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1200[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1200[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1200[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 9) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1200[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1200[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1200[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1200[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1200[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1200[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1200.length === 10) {
            const client1 = dataClientNextDayFour1200[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1200[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1200[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1200[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1200[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1200[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1200[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1200[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1200[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1200[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1200(clientBase);
          };
          if (dataClientNextDayFour1300.length === '') {
            return (setClientsNextDayFour1300(''));
          };
          if (dataClientNextDayFour1300.length === 1) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 2) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 3) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 4) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1300[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 5) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1300[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1300[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 6) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1300[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1300[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1300[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 7) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1300[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1300[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1300[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1300[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 8) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1300[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1300[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1300[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1300[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1300[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 9) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1300[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1300[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1300[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1300[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1300[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1300[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1300.length === 10) {
            const client1 = dataClientNextDayFour1300[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1300[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1300[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1300[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1300[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1300[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1300[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1300[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1300[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1300[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1300(clientBase);
          };
          if (dataClientNextDayFour1400.length === '') {
          return (setClientsNextDayFour1400(''));
          };
          if (dataClientNextDayFour1400.length === 1) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 2) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 3) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 4) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client4 = dataClientNextDayFour1400[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3, client4]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 5) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client4 = dataClientNextDayFour1400[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client5 = dataClientNextDayFour1400[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3, client4, client5]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 6) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client4 = dataClientNextDayFour1400[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client5 = dataClientNextDayFour1400[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client6 = dataClientNextDayFour1400[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3, client4, client5, client6]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 7) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client4 = dataClientNextDayFour1400[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client5 = dataClientNextDayFour1400[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client6 = dataClientNextDayFour1400[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client7 = dataClientNextDayFour1400[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 8) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client4 = dataClientNextDayFour1400[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client5 = dataClientNextDayFour1400[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client6 = dataClientNextDayFour1400[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client7 = dataClientNextDayFour1400[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client8 = dataClientNextDayFour1400[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 9) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client4 = dataClientNextDayFour1400[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client5 = dataClientNextDayFour1400[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client6 = dataClientNextDayFour1400[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client7 = dataClientNextDayFour1400[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client8 = dataClientNextDayFour1400[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client9 = dataClientNextDayFour1400[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1400.length === 10) {
          const client1 = dataClientNextDayFour1400[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client2 = dataClientNextDayFour1400[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client3 = dataClientNextDayFour1400[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client4 = dataClientNextDayFour1400[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client5 = dataClientNextDayFour1400[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client6 = dataClientNextDayFour1400[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client7 = dataClientNextDayFour1400[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client8 = dataClientNextDayFour1400[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client9 = dataClientNextDayFour1400[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const client10 = dataClientNextDayFour1400[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay3; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
          setClientsNextDayFour1400(clientBase);
          };
          if (dataClientNextDayFour1500.length === '') {
            return (setClientsNextDayFour1500(''));
          };
          if (dataClientNextDayFour1500.length === 1) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 2) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 3) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 4) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1500[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 5) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1500[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1500[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 6) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1500[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1500[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1500[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 7) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1500[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1500[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1500[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1500[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 8) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1500[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1500[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1500[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1500[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1500[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 9) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1500[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1500[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1500[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1500[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1500[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1500[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1500.length === 10) {
            const client1 = dataClientNextDayFour1500[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1500[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1500[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1500[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1500[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1500[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1500[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1500[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1500[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1500[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1500(clientBase);
          };
          if (dataClientNextDayFour1600.length === '') {
            return (setClientsNextDayFour1600(''));
          };
          if (dataClientNextDayFour1600.length === 1) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 2) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 3) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 4) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1600[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 5) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1600[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1600[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 6) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1600[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1600[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1600[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 7) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1600[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1600[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1600[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1600[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 8) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1600[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1600[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1600[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1600[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1600[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 9) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1600[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1600[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1600[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1600[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1600[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1600[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1600.length === 10) {
            const client1 = dataClientNextDayFour1600[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1600[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1600[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1600[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1600[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1600[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1600[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1600[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1600[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1600[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1600(clientBase);
          };
          if (dataClientNextDayFour1700.length === '') {
            return (setClientsNextDayFour1700(''));
          };
          if (dataClientNextDayFour1700.length === 1) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 2) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 3) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 4) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1700[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 5) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1700[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1700[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 6) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1700[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1700[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1700[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 7) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1700[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1700[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1700[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1700[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 8) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1700[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1700[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1700[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1700[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1700[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 9) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1700[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1700[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1700[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1700[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1700[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1700[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1700.length === 10) {
            const client1 = dataClientNextDayFour1700[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1700[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1700[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1700[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1700[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1700[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1700[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1700[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1700[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1700[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1700(clientBase);
          };
          if (dataClientNextDayFour1800.length === '') {
            return (setClientsNextDayFour1800(''));
          };
          if (dataClientNextDayFour1800.length === 1) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 2) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 3) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 4) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 5) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 6) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 7) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 8) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1800[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 9) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1800[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1800[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1800.length === 10) {
            const client1 = dataClientNextDayFour1800[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1800[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1800[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1800[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1800[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1800[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1800[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1800[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1800[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1800[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1800(clientBase);
          };
          if (dataClientNextDayFour1900.length === '') {
          return (setClientsNextDayFour1900(''));
          };
          if (dataClientNextDayFour1900.length === 1) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 2) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 3) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 4) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 5) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 6) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 7) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 8) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1900[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 9) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1900[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1900[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour1900.length === 10) {
            const client1 = dataClientNextDayFour1900[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour1900[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour1900[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour1900[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour1900[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour1900[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour1900[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour1900[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour1900[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour1900[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour1900(clientBase);
          };
          if (dataClientNextDayFour2000.length === '') {
            return (setClientsNextDayFour2000(''));
          };
          if (dataClientNextDayFour2000.length === 1) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 2) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 3) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 4) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour2000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 5) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour2000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour2000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 6) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour2000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour2000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour2000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 7) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour2000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour2000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour2000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour2000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 8) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour2000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour2000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour2000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour2000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour2000[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 9) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour2000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour2000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour2000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour2000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour2000[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour2000[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFour2000(clientBase);
          };
          if (dataClientNextDayFour2000.length === 10) {
            const client1 = dataClientNextDayFour2000[0].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client2 = dataClientNextDayFour2000[1].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client3 = dataClientNextDayFour2000[2].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client4 = dataClientNextDayFour2000[3].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client5 = dataClientNextDayFour2000[4].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client6 = dataClientNextDayFour2000[5].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client7 = dataClientNextDayFour2000[6].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client8 = dataClientNextDayFour2000[7].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client9 = dataClientNextDayFour2000[8].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const client10 = dataClientNextDayFour2000[9].info.find((value) => { return value.day === dayOfWeekNextDay3.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay3; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFour2000(clientBase);
          };
// =========================NextDayFive================================================================================================
        const dataClientNextDayFive0800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '08:00'));
        const dataClientNextDayFive0900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '09:00'));
        const dataClientNextDayFive1000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '10:00'));
        const dataClientNextDayFive1100 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '11:00'));
        const dataClientNextDayFive1200 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '12:00'));
        const dataClientNextDayFive1300 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '13:00'));
        const dataClientNextDayFive1400 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '14:00'));
        const dataClientNextDayFive1500 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '15:00'));
        const dataClientNextDayFive1600 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '16:00'));
        const dataClientNextDayFive1700 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '17:00'));
        const dataClientNextDayFive1800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '18:00'));
        const dataClientNextDayFive1900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '19:00'));
        const dataClientNextDayFive2000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay4 && infoDate.time === '20:00'));
        if (dataClientNextDayFive0800.length === '') {
            return (setClientsNextDayFive0800(''));
          };
          if (dataClientNextDayFive0800.length === 1) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 2) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 3) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 4) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 5) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 6) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 7) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 8) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive0800[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 9) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive0800[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive0800[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0800.length === 10) {
            const client1 = dataClientNextDayFive0800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive0800[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive0800[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive0800[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive0800(clientBase);
          };
          if (dataClientNextDayFive0900.length === '') {
            return (setClientsNextDayFive0900(''));
          };
          if (dataClientNextDayFive0900.length === 1) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 2) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 3) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 4) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 5) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 6) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 7) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 8) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive0900[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 9) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive0900[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive0900[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive0900.length === 10) {
            const client1 = dataClientNextDayFive0900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive0900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive0900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive0900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive0900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive0900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive0900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive0900[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive0900[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive0900[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive0900(clientBase);
          };
          if (dataClientNextDayFive1000.length === '') {
            return (setClientsNextDayFive1000(''));
          };
          if (dataClientNextDayFive1000.length === 1) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 2) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 3) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 4) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 5) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 6) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 7) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 8) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1000[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 9) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1000[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1000[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1000.length === 10) {
            const client1 = dataClientNextDayFive1000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1000[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1000[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1000[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1000(clientBase);
          };
          if (dataClientNextDayFive1100.length === '') {
            return (setClientsNextDayFive1100(''));
          };
          if (dataClientNextDayFive1100.length === 1) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 2) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 3) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 4) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1100[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 5) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1100[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1100[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 6) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1100[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1100[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1100[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 7) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1100[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1100[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1100[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1100[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 8) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1100[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1100[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1100[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1100[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1100[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 9) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1100[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1100[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1100[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1100[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1100[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1100[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1100.length === 10) {
            const client1 = dataClientNextDayFive1100[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1100[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1100[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1100[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1100[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1100[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1100[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1100[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1100[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1100[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1100(clientBase);
          };
          if (dataClientNextDayFive1200.length === '') {
            return (setClientsNextDayFive1200(''));
          };
          if (dataClientNextDayFive1200.length === 1) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 2) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 3) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 4) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1200[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 5) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1200[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1200[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 6) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1200[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1200[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1200[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 7) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1200[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1200[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1200[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1200[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 8) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1200[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1200[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1200[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1200[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1200[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 9) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1200[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1200[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1200[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1200[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1200[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1200[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1200.length === 10) {
            const client1 = dataClientNextDayFive1200[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1200[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1200[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1200[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1200[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1200[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1200[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1200[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1200[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1200[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1200(clientBase);
          };
          if (dataClientNextDayFive1300.length === '') {
            return (setClientsNextDayFive1300(''));
          };
          if (dataClientNextDayFive1300.length === 1) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 2) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 3) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 4) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1300[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 5) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1300[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1300[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 6) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1300[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1300[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1300[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 7) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1300[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1300[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1300[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1300[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 8) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1300[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1300[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1300[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1300[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1300[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 9) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1300[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1300[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1300[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1300[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1300[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1300[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1300.length === 10) {
            const client1 = dataClientNextDayFive1300[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1300[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1300[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1300[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1300[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1300[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1300[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1300[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1300[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1300[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1300(clientBase);
          };
          if (dataClientNextDayFive1400.length === '') {
          return (setClientsNextDayFive1400(''));
          };
          if (dataClientNextDayFive1400.length === 1) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 2) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 3) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 4) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client4 = dataClientNextDayFive1400[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3, client4]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 5) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client4 = dataClientNextDayFive1400[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client5 = dataClientNextDayFive1400[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3, client4, client5]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 6) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client4 = dataClientNextDayFive1400[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client5 = dataClientNextDayFive1400[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client6 = dataClientNextDayFive1400[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3, client4, client5, client6]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 7) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client4 = dataClientNextDayFive1400[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client5 = dataClientNextDayFive1400[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client6 = dataClientNextDayFive1400[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client7 = dataClientNextDayFive1400[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 8) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client4 = dataClientNextDayFive1400[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client5 = dataClientNextDayFive1400[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client6 = dataClientNextDayFive1400[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client7 = dataClientNextDayFive1400[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client8 = dataClientNextDayFive1400[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 9) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client4 = dataClientNextDayFive1400[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client5 = dataClientNextDayFive1400[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client6 = dataClientNextDayFive1400[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client7 = dataClientNextDayFive1400[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client8 = dataClientNextDayFive1400[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client9 = dataClientNextDayFive1400[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1400.length === 10) {
          const client1 = dataClientNextDayFive1400[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client2 = dataClientNextDayFive1400[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client3 = dataClientNextDayFive1400[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client4 = dataClientNextDayFive1400[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client5 = dataClientNextDayFive1400[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client6 = dataClientNextDayFive1400[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client7 = dataClientNextDayFive1400[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client8 = dataClientNextDayFive1400[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client9 = dataClientNextDayFive1400[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const client10 = dataClientNextDayFive1400[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay4; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
          setClientsNextDayFive1400(clientBase);
          };
          if (dataClientNextDayFive1500.length === '') {
            return (setClientsNextDayFive1500(''));
          };
          if (dataClientNextDayFive1500.length === 1) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 2) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 3) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 4) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1500[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 5) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1500[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1500[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 6) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1500[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1500[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1500[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 7) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1500[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1500[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1500[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1500[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 8) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1500[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1500[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1500[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1500[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1500[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 9) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1500[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1500[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1500[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1500[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1500[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1500[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1500.length === 10) {
            const client1 = dataClientNextDayFive1500[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1500[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1500[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1500[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1500[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1500[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1500[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1500[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1500[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1500[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1500(clientBase);
          };
          if (dataClientNextDayFive1600.length === '') {
            return (setClientsNextDayFive1600(''));
          };
          if (dataClientNextDayFive1600.length === 1) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 2) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 3) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 4) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1600[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 5) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1600[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1600[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 6) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1600[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1600[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1600[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 7) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1600[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1600[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1600[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1600[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 8) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1600[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1600[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1600[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1600[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1600[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 9) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1600[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1600[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1600[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1600[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1600[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1600[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1600.length === 10) {
            const client1 = dataClientNextDayFive1600[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1600[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1600[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1600[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1600[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1600[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1600[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1600[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1600[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1600[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1600(clientBase);
          };
          if (dataClientNextDayFive1700.length === '') {
            return (setClientsNextDayFive1700(''));
          };
          if (dataClientNextDayFive1700.length === 1) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 2) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 3) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 4) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1700[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 5) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1700[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1700[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 6) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1700[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1700[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1700[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 7) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1700[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1700[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1700[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1700[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 8) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1700[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1700[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1700[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1700[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1700[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 9) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1700[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1700[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1700[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1700[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1700[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1700[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1700.length === 10) {
            const client1 = dataClientNextDayFive1700[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1700[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1700[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1700[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1700[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1700[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1700[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1700[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1700[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1700[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1700(clientBase);
          };
          if (dataClientNextDayFive1800.length === '') {
            return (setClientsNextDayFive1800(''));
          };
          if (dataClientNextDayFive1800.length === 1) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 2) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 3) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 4) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 5) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 6) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 7) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 8) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1800[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 9) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1800[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1800[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1800.length === 10) {
            const client1 = dataClientNextDayFive1800[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1800[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1800[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1800[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1800[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1800[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1800[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1800[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1800[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1800[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1800(clientBase);
          };
          if (dataClientNextDayFive1900.length === '') {
          return (setClientsNextDayFive1900(''));
          };
          if (dataClientNextDayFive1900.length === 1) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 2) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 3) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 4) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 5) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 6) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 7) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 8) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1900[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 9) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1900[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1900[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive1900.length === 10) {
            const client1 = dataClientNextDayFive1900[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive1900[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive1900[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive1900[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive1900[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive1900[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive1900[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive1900[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive1900[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive1900[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive1900(clientBase);
          };
          if (dataClientNextDayFive2000.length === '') {
            return (setClientsNextDayFive2000(''));
          };
          if (dataClientNextDayFive2000.length === 1) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 2) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 3) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 4) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive2000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 5) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive2000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive2000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 6) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive2000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive2000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive2000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 7) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive2000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive2000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive2000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive2000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 8) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive2000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive2000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive2000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive2000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive2000[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 9) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive2000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive2000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive2000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive2000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive2000[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive2000[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDayFive2000(clientBase);
          };
          if (dataClientNextDayFive2000.length === 10) {
            const client1 = dataClientNextDayFive2000[0].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client2 = dataClientNextDayFive2000[1].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client3 = dataClientNextDayFive2000[2].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client4 = dataClientNextDayFive2000[3].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client5 = dataClientNextDayFive2000[4].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client6 = dataClientNextDayFive2000[5].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client7 = dataClientNextDayFive2000[6].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client8 = dataClientNextDayFive2000[7].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client9 = dataClientNextDayFive2000[8].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const client10 = dataClientNextDayFive2000[9].info.find((value) => { return value.day === dayOfWeekNextDay4.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay4; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDayFive2000(clientBase);
          };
// =========================NextDaySix================================================================================================
        const dataClientNextDaySix0800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '08:00'));
        const dataClientNextDaySix0900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '09:00'));
        const dataClientNextDaySix1000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '10:00'));
        const dataClientNextDaySix1100 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '11:00'));
        const dataClientNextDaySix1200 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '12:00'));
        const dataClientNextDaySix1300 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '13:00'));
        const dataClientNextDaySix1400 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '14:00'));
        const dataClientNextDaySix1500 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '15:00'));
        const dataClientNextDaySix1600 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '16:00'));
        const dataClientNextDaySix1700 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '17:00'));
        const dataClientNextDaySix1800 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '18:00'));
        const dataClientNextDaySix1900 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '19:00'));
        const dataClientNextDaySix2000 = result.filter(arr => arr.info.some(infoDate => infoDate.date.slice(0, 10) === dateNextDay5 && infoDate.time === '20:00'));
        if (dataClientNextDaySix0800.length === '') {
            return (setClientsNextDaySix0800(''));
          };
          if (dataClientNextDaySix0800.length === 1) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 2) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 3) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 4) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 5) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 6) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 7) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 8) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix0800[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 9) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix0800[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix0800[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix0800(clientBase);
          };
          if (dataClientNextDaySix0800.length === 10) {
            const client1 = dataClientNextDaySix0800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix0800[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix0800[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix0800[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '08:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix0800(clientBase);
        };
        if (dataClientNextDaySix0900.length === '') {
            return (setClientsNextDaySix0900(''));
          };
          if (dataClientNextDaySix0900.length === 1) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 2) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 3) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 4) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 5) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 6) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 7) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 8) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix0900[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 9) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix0900[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix0900[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix0900.length === 10) {
            const client1 = dataClientNextDaySix0900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix0900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix0900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix0900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix0900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix0900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix0900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix0900[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix0900[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix0900[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '09:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix0900(clientBase);
          };
          if (dataClientNextDaySix1000.length === '') {
            return (setClientsNextDaySix1000(''));
          };
          if (dataClientNextDaySix1000.length === 1) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 2) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 3) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 4) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 5) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 6) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 7) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 8) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1000[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 9) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1000[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1000[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1000.length === 10) {
            const client1 = dataClientNextDaySix1000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1000[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1000[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1000[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '10:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1000(clientBase);
          };
          if (dataClientNextDaySix1100.length === '') {
            return (setClientsNextDaySix1100(''));
          };
          if (dataClientNextDaySix1100.length === 1) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 2) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 3) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 4) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1100[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 5) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1100[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1100[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 6) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1100[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1100[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1100[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 7) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1100[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1100[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1100[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1100[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 8) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1100[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1100[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1100[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1100[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1100[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 9) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1100[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1100[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1100[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1100[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1100[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1100[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1100.length === 10) {
            const client1 = dataClientNextDaySix1100[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1100[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1100[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1100[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1100[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1100[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1100[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1100[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1100[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1100[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '11:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1100(clientBase);
          };
          if (dataClientNextDaySix1200.length === '') {
            return (setClientsNextDaySix1200(''));
          };
          if (dataClientNextDaySix1200.length === 1) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 2) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 3) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 4) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1200[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 5) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1200[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1200[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 6) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1200[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1200[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1200[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 7) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1200[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1200[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1200[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1200[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 8) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1200[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1200[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1200[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1200[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1200[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 9) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1200[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1200[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1200[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1200[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1200[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1200[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1200.length === 10) {
            const client1 = dataClientNextDaySix1200[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1200[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1200[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1200[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1200[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1200[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1200[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1200[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1200[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1200[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '12:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1200(clientBase);
          };
          if (dataClientNextDaySix1300.length === '') {
            return (setClientsNextDaySix1300(''));
          };
          if (dataClientNextDaySix1300.length === 1) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 2) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 3) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 4) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1300[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 5) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1300[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1300[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 6) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1300[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1300[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1300[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 7) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1300[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1300[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1300[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1300[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 8) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1300[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1300[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1300[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1300[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1300[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 9) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1300[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1300[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1300[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1300[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1300[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1300[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1300.length === 10) {
            const client1 = dataClientNextDaySix1300[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1300[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1300[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1300[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1300[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1300[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1300[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1300[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1300[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1300[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '13:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1300(clientBase);
          };
          if (dataClientNextDaySix1400.length === '') {
          return (setClientsNextDaySix1400(''));
          };
          if (dataClientNextDaySix1400.length === 1) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 2) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 3) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 4) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client4 = dataClientNextDaySix1400[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3, client4]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 5) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client4 = dataClientNextDaySix1400[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client5 = dataClientNextDaySix1400[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3, client4, client5]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 6) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client4 = dataClientNextDaySix1400[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client5 = dataClientNextDaySix1400[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client6 = dataClientNextDaySix1400[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3, client4, client5, client6]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 7) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client4 = dataClientNextDaySix1400[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client5 = dataClientNextDaySix1400[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client6 = dataClientNextDaySix1400[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client7 = dataClientNextDaySix1400[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 8) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client4 = dataClientNextDaySix1400[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client5 = dataClientNextDaySix1400[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client6 = dataClientNextDaySix1400[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client7 = dataClientNextDaySix1400[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client8 = dataClientNextDaySix1400[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 9) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client4 = dataClientNextDaySix1400[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client5 = dataClientNextDaySix1400[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client6 = dataClientNextDaySix1400[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client7 = dataClientNextDaySix1400[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client8 = dataClientNextDaySix1400[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client9 = dataClientNextDaySix1400[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1400.length === 10) {
          const client1 = dataClientNextDaySix1400[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client2 = dataClientNextDaySix1400[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client3 = dataClientNextDaySix1400[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client4 = dataClientNextDaySix1400[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client5 = dataClientNextDaySix1400[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client6 = dataClientNextDaySix1400[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client7 = dataClientNextDaySix1400[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client8 = dataClientNextDaySix1400[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client9 = dataClientNextDaySix1400[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const client10 = dataClientNextDaySix1400[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '14:00' && value.date.slice(0, 10) === dateNextDay5; });
          const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
          setClientsNextDaySix1400(clientBase);
          };
          if (dataClientNextDaySix1500.length === '') {
            return (setClientsNextDaySix1500(''));
          };
          if (dataClientNextDaySix1500.length === 1) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 2) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 3) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 4) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1500[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 5) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1500[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1500[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 6) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1500[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1500[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1500[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 7) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1500[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1500[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1500[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1500[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 8) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1500[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1500[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1500[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1500[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1500[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 9) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1500[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1500[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1500[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1500[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1500[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1500[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1500.length === 10) {
            const client1 = dataClientNextDaySix1500[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1500[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1500[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1500[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1500[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1500[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1500[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1500[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1500[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1500[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '15:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1500(clientBase);
          };
          if (dataClientNextDaySix1600.length === '') {
            return (setClientsNextDaySix1600(''));
          };
          if (dataClientNextDaySix1600.length === 1) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 2) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 3) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 4) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1600[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 5) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1600[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1600[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 6) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1600[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1600[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1600[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 7) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1600[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1600[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1600[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1600[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 8) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1600[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1600[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1600[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1600[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1600[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 9) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1600[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1600[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1600[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1600[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1600[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1600[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1600.length === 10) {
            const client1 = dataClientNextDaySix1600[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1600[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1600[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1600[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1600[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1600[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1600[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1600[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1600[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1600[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '16:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1600(clientBase);
          };
          if (dataClientNextDaySix1700.length === '') {
            return (setClientsNextDaySix1700(''));
          };
          if (dataClientNextDaySix1700.length === 1) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 2) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 3) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 4) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1700[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 5) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1700[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1700[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 6) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1700[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1700[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1700[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 7) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1700[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1700[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1700[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1700[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 8) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1700[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1700[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1700[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1700[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1700[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 9) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1700[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1700[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1700[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1700[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1700[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1700[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1700.length === 10) {
            const client1 = dataClientNextDaySix1700[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1700[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1700[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1700[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1700[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1700[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1700[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1700[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1700[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1700[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '17:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1700(clientBase);
          };
          if (dataClientNextDaySix1800.length === '') {
            return (setClientsNextDaySix1800(''));
          };
          if (dataClientNextDaySix1800.length === 1) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 2) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 3) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 4) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 5) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 6) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 7) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 8) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1800[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 9) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1800[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1800[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1800.length === 10) {
            const client1 = dataClientNextDaySix1800[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1800[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1800[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1800[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1800[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1800[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1800[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1800[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1800[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1800[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '18:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1800(clientBase);
          };
          if (dataClientNextDaySix1900.length === '') {
          return (setClientsNextDaySix1900(''));
          };
          if (dataClientNextDaySix1900.length === 1) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 2) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 3) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 4) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 5) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 6) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 7) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 8) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1900[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 9) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1900[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1900[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix1900.length === 10) {
            const client1 = dataClientNextDaySix1900[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix1900[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix1900[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix1900[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix1900[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix1900[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix1900[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix1900[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix1900[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix1900[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '19:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix1900(clientBase);
          };
          if (dataClientNextDaySix2000.length === '') {
            return (setClientsNextDaySix2000(''));
          };
          if (dataClientNextDaySix2000.length === 1) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 2) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 3) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 4) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix2000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 5) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix2000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix2000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 6) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix2000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix2000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix2000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 7) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix2000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix2000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix2000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix2000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 8) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix2000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix2000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix2000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix2000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix2000[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 9) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix2000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix2000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix2000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix2000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix2000[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix2000[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9]
            setClientsNextDaySix2000(clientBase);
          };
          if (dataClientNextDaySix2000.length === 10) {
            const client1 = dataClientNextDaySix2000[0].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client2 = dataClientNextDaySix2000[1].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client3 = dataClientNextDaySix2000[2].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client4 = dataClientNextDaySix2000[3].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client5 = dataClientNextDaySix2000[4].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client6 = dataClientNextDaySix2000[5].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client7 = dataClientNextDaySix2000[6].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client8 = dataClientNextDaySix2000[7].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client9 = dataClientNextDaySix2000[8].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const client10 = dataClientNextDaySix2000[9].info.find((value) => { return value.day === dayOfWeekNextDay5.toLocaleLowerCase() && value.time === '20:00' && value.date.slice(0, 10) === dateNextDay5; });
            const clientBase = [client1, client2, client3, client4, client5, client6, client7, client8, client9, client10]
            setClientsNextDaySix2000(clientBase);
          };
        // if (dayOfWeekToday.toLocaleLowerCase() === 'monday') {
        //   return[
        //     setToday('Понеділок'),
        //     setNextDay('Вівторок'),
        //     setNextDay1('Середа'),
        //     setNextDay2('Четвер'),
        //     setNextDay3('П"ятниця'),
        //     setNextDay4('Субота'),
        //     setNextDay5('Неділя'),
        //   ]
        // }
        // if (dayOfWeekToday.toLocaleLowerCase() === 'tuesday') {
        //   return[
        //     setToday('Вівторок'),
        //     setNextDay('Середа'),
        //     setNextDay1('Четвер'),
        //     setNextDay2('П"ятниця'),
        //     setNextDay3('Субота'),
        //     setNextDay4('Неділя'),
        //     setNextDay5('Понеділок'),
        //   ]
        // }
        // if (dayOfWeekToday.toLocaleLowerCase() === 'wednesday') {
        //   return[
        //     setToday('Середа'),
        //     setNextDay('Четвер'),
        //     setNextDay1('П"ятниця'),
        //     setNextDay2('Субота'),
        //     setNextDay3('Неділя'),
        //     setNextDay4('Понеділок'),
        //     setNextDay5('Вівторок'),
        //   ]
        // }
        // if (dayOfWeekToday.toLocaleLowerCase() === 'thursday') {
        //   return[
        //     setToday('Четвер'),
        //     setNextDay('П"ятниця'),
        //     setNextDay1('Субота'),
        //     setNextDay2('Неділя'),
        //     setNextDay3('Понеділок'),
        //     setNextDay4('Вівторок'),
        //     setNextDay5('Середа'),
        //   ]
        // }
        // if (dayOfWeekToday.toLocaleLowerCase() === 'friday') {
        //   return[
        //     setToday('П"ятниця'),
        //     setNextDay('Субота'),
        //     setNextDay1('Неділя'),
        //     setNextDay2('Понеділок'),
        //     setNextDay3('Вівторок'),
        //     setNextDay4('Середа'),
        //     setNextDay5('Четвер'),
        //   ]
        // }
        // if (dayOfWeekToday.toLocaleLowerCase() === 'saturday') {
        //   return[
        //     setToday('Субота'),
        //     setNextDay('Неділя'),
        //     setNextDay1('Понеділок'),
        //     setNextDay2('Вівторок'),
        //     setNextDay3('Середа'),
        //     setNextDay4('Четвер'),
        //     setNextDay5('П"ятниця'),
        //   ]
        // }
        // if (dayOfWeekToday.toLocaleLowerCase() === 'sunday') {
        //   return[
        //     setToday('Неділя'),
        //     setNextDay('Понеділок'),
        //     setNextDay1('Вівторок'),
        //     setNextDay2('Середа'),
        //     setNextDay3('Четвер'),
        //     setNextDay4('П"ятниця'),
        //     setNextDay5('Субота'),
        //   ]
        // }
        setDataAllClients(dayOfWeekToday);
              // console.log(dayOfWeekNextDay);
              // console.log(dataClientNextDay1400[0].info[2].name);
    })}
  }, [dataAllClients, clientsToday0800, clientsToday0900, clientsToday1000, clientsToday1100, clientsToday1200, clientsToday1300,
    clientsToday1400, clientsToday1500, clientsToday1600, clientsToday1700, clientsToday1800, clientsToday1900, clientsToday2000,
  clientsNextDay0800, clientsNextDay0900, clientsNextDay1000, clientsNextDay1100, clientsNextDay1200, clientsNextDay1300,
    clientsNextDay1400, clientsNextDay1500, clientsNextDay1600, clientsNextDay1700, clientsNextDay1800, clientsNextDay1900, clientsToday2000])
          // console.log(clientsNextDay1400);
  useEffect(() => {
    const dayOfWeekToday = moment().add(0, 'days').format('dddd');
    const dayTranslate = () => {
    if (dayOfWeekToday.toLocaleLowerCase() === 'monday') {
          return[
            setToday('Понеділок'),
            setNextDay('Вівторок'),
            setNextDay1('Середа'),
            setNextDay2('Четвер'),
            setNextDay3('П`ятниця'),
            setNextDay4('Субота'),
            setNextDay5('Неділя'),
          ]
        }
        if (dayOfWeekToday.toLocaleLowerCase() === 'tuesday') {
          return[
            setToday('Вівторок'),
            setNextDay('Середа'),
            setNextDay1('Четвер'),
            setNextDay2('П`ятниця'),
            setNextDay3('Субота'),
            setNextDay4('Неділя'),
            setNextDay5('Понеділок'),
          ]
        }
        if (dayOfWeekToday.toLocaleLowerCase() === 'wednesday') {
          return[
            setToday('Середа'),
            setNextDay('Четвер'),
            setNextDay1('П`ятниця'),
            setNextDay2('Субота'),
            setNextDay3('Неділя'),
            setNextDay4('Понеділок'),
            setNextDay5('Вівторок'),
          ]
        }
        if (dayOfWeekToday.toLocaleLowerCase() === 'thursday') {
          return[
            setToday('Четвер'),
            setNextDay('П`ятниця'),
            setNextDay1('Субота'),
            setNextDay2('Неділя'),
            setNextDay3('Понеділок'),
            setNextDay4('Вівторок'),
            setNextDay5('Середа'),
          ]
        }
        if (dayOfWeekToday.toLocaleLowerCase() === 'friday') {
          return[
            setToday('П`ятниця'),
            setNextDay('Субота'),
            setNextDay1('Неділя'),
            setNextDay2('Понеділок'),
            setNextDay3('Вівторок'),
            setNextDay4('Середа'),
            setNextDay5('Четвер'),
          ]
        }
        if (dayOfWeekToday.toLocaleLowerCase() === 'saturday') {
          return[
            setToday('Субота'),
            setNextDay('Неділя'),
            setNextDay1('Понеділок'),
            setNextDay2('Вівторок'),
            setNextDay3('Середа'),
            setNextDay4('Четвер'),
            setNextDay5('П`ятниця'),
          ]
        }
        if (dayOfWeekToday.toLocaleLowerCase() === 'sunday') {
          return[
            setToday('Неділя'),
            setNextDay('Понеділок'),
            setNextDay1('Вівторок'),
            setNextDay2('Середа'),
            setNextDay3('Четвер'),
            setNextDay4('П`ятниця'),
            setNextDay5('Субота'),
          ]
        }
      };
      dayTranslate();
  }, [today]);
  // const instaNickName = '12121212';
  // const urlInsta = `https://www.instagram.com/${instaNickName.substring(1)}/`
  const URLInsta = ({ item }) => {
    // console.log(item)
    const urlInstagram = `https://www.instagram.com/${item.instaNickName.substring(1)}`
    if (item.instaNickName === undefined) {
      // console.log("DADADA")
      const notFound = `https://www.instagram.com/`;
      return (notFound);
    }
    return (urlInstagram);
  };

  const ContinueBTN = () => {
    if (continueView === true) {
      return(setContinueView(false));
    }
    setContinueView(true);
  };
  // console.log("clientsNextDayTwo1400: ", clientsNextDayTwo1400)

  return(
    <div
      // style={{ display: 'flex', flexDirection: 'column' }}
      style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', width: '100%' }}
    >
      <div>
        {/* <DayOfWeekWrapper><p>День/</p><p>Час</p></DayOfWeekWrapper> */}
        {continueView ?
        <WrapperBTN>
          <NextDaysBTN onClick={ContinueBTN}>Next</NextDaysBTN>
        </WrapperBTN>
        :
        <WrapperBTN>
          <NextDaysBTN onClick={ContinueBTN}>Back</NextDaysBTN>
        </WrapperBTN>}
        <WrapperInfoTime><p>8:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>9:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>10:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>11:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>12:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>13:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>14:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>15:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>16:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>17:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>18:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>19:00</p></WrapperInfoTime>
        <WrapperInfoTime><p>20:00</p></WrapperInfoTime>
      </div>
      
      {continueView ?
        <>
          <SortingFirstRender
            URLInsta={URLInsta}
            clientsToday0800={clientsToday0800}
            clientsToday0900={clientsToday0900}
            clientsToday1000={clientsToday1000}
            clientsToday1100={clientsToday1100}
            clientsToday1200={clientsToday1200}
            clientsToday1300={clientsToday1300}
            clientsToday1400={clientsToday1400}
            clientsToday1500={clientsToday1500}
            clientsToday1600={clientsToday1600}
            clientsToday1700={clientsToday1700}
            clientsToday1800={clientsToday1800}
            clientsToday1900={clientsToday1900}
            clientsToday2000={clientsToday2000}
            clientsNextDay0800={clientsNextDay0800}
            clientsNextDay0900={clientsNextDay0900}
            clientsNextDay1000={clientsNextDay1000}
            clientsNextDay1100={clientsNextDay1100}
            clientsNextDay1200={clientsNextDay1200}
            clientsNextDay1300={clientsNextDay1300}
            clientsNextDay1400={clientsNextDay1400}
            clientsNextDay1500={clientsNextDay1500}
            clientsNextDay1600={clientsNextDay1600}
            clientsNextDay1700={clientsNextDay1700}
            clientsNextDay1800={clientsNextDay1800}
            clientsNextDay1900={clientsNextDay1900}
            clientsNextDay2000={clientsNextDay2000}
            clientsNextDayTwo0800={clientsNextDayTwo0800}
            clientsNextDayTwo0900={clientsNextDayTwo0900}
            clientsNextDayTwo1000={clientsNextDayTwo1000}
            clientsNextDayTwo1100={clientsNextDayTwo1100}
            clientsNextDayTwo1200={clientsNextDayTwo1200}
            clientsNextDayTwo1300={clientsNextDayTwo1300}
            clientsNextDayTwo1400={clientsNextDayTwo1400}
            clientsNextDayTwo1500={clientsNextDayTwo1500}
            clientsNextDayTwo1600={clientsNextDayTwo1600}
            clientsNextDayTwo1700={clientsNextDayTwo1700}
            clientsNextDayTwo1800={clientsNextDayTwo1800}
            clientsNextDayTwo1900={clientsNextDayTwo1900}
            clientsNextDayTwo2000={clientsNextDayTwo2000}
            today={today}
            nextDay={nextDay}
            nextDay1={nextDay1}
          />
        </>
        :
        <>
          <SortingSecondRender
            URLInsta={URLInsta}
            clientsNextDayThree0800={clientsNextDayThree0800}
            clientsNextDayThree0900={clientsNextDayThree0900}
            clientsNextDayThree1000={clientsNextDayThree1000}
            clientsNextDayThree1100={clientsNextDayThree1100}
            clientsNextDayThree1200={clientsNextDayThree1200}
            clientsNextDayThree1300={clientsNextDayThree1300}
            clientsNextDayThree1400={clientsNextDayThree1400}
            clientsNextDayThree1500={clientsNextDayThree1500}
            clientsNextDayThree1600={clientsNextDayThree1600}
            clientsNextDayThree1700={clientsNextDayThree1700}
            clientsNextDayThree1800={clientsNextDayThree1800}
            clientsNextDayThree1900={clientsNextDayThree1900}
            clientsNextDayThree2000={clientsNextDayThree2000}
            clientsNextDayFour0800={clientsNextDayFour0800}
            clientsNextDayFour0900={clientsNextDayFour0900}
            clientsNextDayFour1000={clientsNextDayFour1000}
            clientsNextDayFour1100={clientsNextDayFour1100}
            clientsNextDayFour1200={clientsNextDayFour1200}
            clientsNextDayFour1300={clientsNextDayFour1300}
            clientsNextDayFour1400={clientsNextDayFour1400}
            clientsNextDayFour1500={clientsNextDayFour1500}
            clientsNextDayFour1600={clientsNextDayFour1600}
            clientsNextDayFour1700={clientsNextDayFour1700}
            clientsNextDayFour1800={clientsNextDayFour1800}
            clientsNextDayFour1900={clientsNextDayFour1900}
            clientsNextDayFour2000={clientsNextDayFour2000}
            clientsNextDayFive0800={clientsNextDayFive0800}
            clientsNextDayFive0900={clientsNextDayFive0900}
            clientsNextDayFive1000={clientsNextDayFive1000}
            clientsNextDayFive1100={clientsNextDayFive1100}
            clientsNextDayFive1200={clientsNextDayFive1200}
            clientsNextDayFive1300={clientsNextDayFive1300}
            clientsNextDayFive1400={clientsNextDayFive1400}
            clientsNextDayFive1500={clientsNextDayFive1500}
            clientsNextDayFive1600={clientsNextDayFive1600}
            clientsNextDayFive1700={clientsNextDayFive1700}
            clientsNextDayFive1800={clientsNextDayFive1800}
            clientsNextDayFive1900={clientsNextDayFive1900}
            clientsNextDayFive2000={clientsNextDayFive2000}
            clientsNextDaySix0800={clientsNextDaySix0800}
            clientsNextDaySix0900={clientsNextDaySix0900}
            clientsNextDaySix1000={clientsNextDaySix1000}
            clientsNextDaySix1100={clientsNextDaySix1100}
            clientsNextDaySix1200={clientsNextDaySix1200}
            clientsNextDaySix1300={clientsNextDaySix1300}
            clientsNextDaySix1400={clientsNextDaySix1400}
            clientsNextDaySix1500={clientsNextDaySix1500}
            clientsNextDaySix1600={clientsNextDaySix1600}
            clientsNextDaySix1700={clientsNextDaySix1700}
            clientsNextDaySix1800={clientsNextDaySix1800}
            clientsNextDaySix1900={clientsNextDaySix1900}
            clientsNextDaySix2000={clientsNextDaySix2000}
            nextDay2={nextDay2}
            nextDay3={nextDay3}
            nextDay4={nextDay4}
            nextDay5={nextDay5}
          />
        </>
      }      
    </div>
  )}

export default SortingTrainee;